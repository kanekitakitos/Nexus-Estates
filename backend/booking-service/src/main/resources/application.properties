spring.application.name=booking-service
server.port=8081

# Base de Dados de Reservas
spring.datasource.url=jdbc:postgresql://localhost:5432/booking_db
spring.datasource.username=admin
spring.datasource.password=password

# MODO PRODUÇÃO: Hibernate apenas valida se a estrutura
spring.jpa.hibernate.ddl-auto=validate
# Flyway ativado para gestÃ£o de versões da BD
spring.flyway.enabled=true
# LocalizaÃ§Ã£o das migrações (default)
spring.flyway.locations=classpath:db/migration

# Opcional: Mostra o SQL gerado no log para debug
spring.jpa.show-sql=true

# Configuração RabbitMQ
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest